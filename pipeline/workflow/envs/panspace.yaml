name: panspace
channels:
  - conda-forge
  - anaconda
dependencies:
  - python >= 3.9, < 3.11
  - pip
  - pip:
    - -e /home/avila/github/panspace # git+https://github.com/pg-space/panspace.git # install panspace package from setup.cfg